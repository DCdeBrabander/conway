{
  "scripts": {
    "build": "node ./.esbuild/build.mjs",
    "watch": "node ./.esbuild/watch.mjs",
    "start": "npm run build && firefox ./index.html",
    "browser-sync": "browser-sync start --server --files ."
  },
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "esbuild": "0.19.3",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
